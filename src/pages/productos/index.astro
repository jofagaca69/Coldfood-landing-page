---
import Layout from '@/layouts/Layout.astro';
import { CATEGORIES } from '@/data/categories';
---

<Layout title="Productos | Coldfood" description="Catálogo de productos Coldfood por categoría.">
	<main class="mx-auto max-w-5xl px-4 py-16">
		<h1 class="text-center text-3xl font-bold text-primary sm:text-5xl">Productos</h1>
		<p class="mt-4 text-center text-ink-muted">Contenido próximamente.</p>

		<div class="mt-16 space-y-24">
			{
				CATEGORIES.map((cat) => (
					<section id={cat.slug} class="scroll-mt-24 border-t border-line pt-10">
						<h2 class="text-2xl font-semibold text-ink">{cat.es}</h2>
						<p class="mt-2 text-ink-muted">Contenido próximamente.</p>
					</section>
				))
			}
		</div>
	</main>
</Layout>
